namespace Mette_miniBank.Types
{
    // Enum, der repræsenterer forskellige valutatyper
    public enum Currency
    {
        DKK = 1,  // god stil at specificere int værdien af hver enum værdi, for at det ikke går galt når man tilføjer nye værdier (eg. sætter øverst)
        EUR = 2,
    }
}
